apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: <%= @pvc_name %>
  annotations:
    volume.beta.kubernetes.io/storage-class: "slow"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
